#include<iostream>
using namespace std;

    //gets(str)输入字符串 遇到换行符停止
	//scanf（"%s"，str）输入字符串忽略 空格；
	
	//puts() 输出字符串 自动输出一个'\n'
	
	//string() 计算字符串s 的长度，不包含'\0' 
    
    //strcpy( , ) 把后字符串拷贝到前面的 包括'\0'
	
	//strcat ( , ) 把俩个字符串拼接起来；；；
	
	// strcmp ( , ) 比较连个字符串的大小 比较的是ASCII的码的大小
	
	// strchr (s,c) 在s中寻找并返回 第一个c的地址 
	
	// strstr ( ,s) 在字符串中寻找并返回 第一次s的 首地址 
    

